export const TOKEN = 'bingo-token'

export enum Role {
	GAME_MASTER = 'GAME_MASTER',
	PLAYER = 'PLAYER'
}

export enum State {
	SETUP = 'SETUP',
	LOCKED = 'LOCKED',
	RUNNING = 'RUNNING',
	DONE = 'DONE'
}
